# P0.11: Five-Module Adapter Or Experimental-Gate Decision

- **Change:** `create-metaphysics-chart-ui-package`
- **QiZheng baseline:** `f130449`
- **Package gate commit:** `977211ac98fe`
- **Date:** 2026-06-20

## Adapter Inventory

| Module | Real adapter | Production consumer |
| --- | --- | --- |
| QiZhengSiYu | `lib/presentation/chart_adapters/qizheng_chart_board_adapter.dart` | Yes |
| QiMenDunJia | None | No |
| DaLiuRen | None | No |
| ZiWeiDouShu | None | No |
| TaiYiShenShu | None | No |

The five-adapter path is incomplete, so P0.11 uses the explicitly permitted
experimental-API path. P0.6 source-shape mappings do not count as adapters.

## Experimental Marker

Package commit `ea37cd7c85fd` declares `@experimental` immediately before the
public `library;` directive in `lib/metaphysics_chart_ui.dart`.

## Executable Second-Migration Gate

Package commit `977211ac98fe` replaces the previous self-fulfilling test with a
workspace-backed gate. `second_migration_gate_test.dart` now:

1. Reads the real public barrel and requires `@experimental\s+library;`.
2. Parses every sibling module `pubspec.yaml` and discovers actual
   `metaphysics_chart_ui` consumers from dependencies and overrides.
3. Allows QiZhengSiYu as the sole production consumer.
4. Rejects a second consumer unless `multi_module_conformance_test.dart`
   exists, declares tests with hard `expect` assertions, and names every
   discovered consumer.
5. Includes negative fixtures for a missing annotation, a second consumer with
   no suite, and an empty suite.

Fresh verification at package commit `977211ac98fe`:

- `flutter analyze`: no issues.
- `flutter test`: 134 tests passed.
- Gate-focused run: four tests passed, including three negative fixtures.
- Placeholder package smoke test replaced by a public-barrel contract test.

## Boundary

Passing P0.11 authorizes work on the package while it remains experimental. It
does not authorize another module migration. A second consumer requires the
real multi-module conformance suite and a full package test run.

## Verdict

**PASS via the experimental path.** The limitation is represented by both a
public API marker and a non-self-referential executable dependency gate.
