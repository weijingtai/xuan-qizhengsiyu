# P0.3: QiMenDunJia SmartQiMenGrid Baseline

- **Change:** `create-metaphysics-chart-ui-package`
- **Commit baseline:** `f130449`
- **Date:** 2026-06-20

## Source Files

| File | Lines | Description |
|------|-------|-------------|
| `xuan-qimendunjia/lib/redesign_ui/layouts/smart_grid.dart` | 602 | `SmartQiMenGrid` + `SmartPalaceWidget` + `PalaceData` |
| `xuan-qimendunjia/lib/redesign_ui/components/palace/brief_palace_layout.dart` | 1292 | `BriefPalaceLayout` 3-column Row per cell |

## Widget Structure

```
SmartQiMenGrid (StatelessWidget, smart_grid.dart:19)
  └─ LayoutBuilder (line 39)
       └─ Container (line 55) — outer frame, background, border, shadow, radius
            └─ GridView.builder (line 92)
                 ├─ crossAxisCount: 3, childAspectRatio: 1.0
                 ├─ crossAxisSpacing: 0, mainAxisSpacing: 0
                 ├─ itemCount: 9 (fixed 3×3)
                 └─ itemBuilder → SmartPalaceWidget (line 105)
                      └─ GestureDetector (line 175)
                           └─ Container (per-palace cell)
                                └─ BriefPalaceLayout (3-column Row)
```

## Theme Usage

- Grid: `XuanThemeData.maybeOf(context)?.component('qimen_palace_grid')` (smart_grid.dart:45)
- Cell: `XuanThemeData.maybeOf(context)?.component('qimen_palace_cell')` (brief_palace_layout.dart:35)
  - Resolved via `BriefPalaceTheme.fromComponent(cellStyle)` (line 37)
- Fallback: hardcoded colors when XuanThemeScope not present

## Neutral Model Mapping

- 3×3 grid → `CellLayer` with 9 `ChartSector` entries (row/column)
- PalaceData → ChartSector.items (TextItem, BadgeItem, IconItem)
- Theme tokens → styleRole + BoardTheme module palette
- GestureDetector.onTap → BoardInteractionController tap selection

## Callers

- `xuan-qimendunjia/lib/presentation/pages/qimen_mvvm_page.dart:498`
- `xuan-qimendunjia/lib/presentation/pages/multi_jia_qimen_page.dart:505`
