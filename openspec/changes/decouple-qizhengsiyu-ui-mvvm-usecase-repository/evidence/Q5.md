# Q5 Pure Domain And Legacy Cleanup Evidence

- **Date:** 2026-06-14
- **Branch:** `codex/qizhengsiyu-mvvm-usecase-repository-plan`
- **Result:** GO

## Verification

### 5.1 SwephEngine — rootBundle removed
- `SwephEngine` constructor now requires `QiZhengEphemerisResourceRepository`.
- `getSystemDefinition()` delegates resource loading to `_ephemerisRes.loadEphemerisResource(assertName)` instead of `rootBundle.loadString()`.
- `di.dart` updated: `SwephEngine(ephemerisRes: _deps.ephemerisResource)`.
- Data reads the same JSON files via `AssetsQiZhengEphemerisResourceRepository` (same asset paths, same `rootBundle.loadString` in storage layer).

### 5.2 HistoricalEngine — already clean from Q3
- Zero `rootBundle` or `package:flutter/services.dart` imports. Confirmed.

### 5.3 YuanLePanelBuilder — rootBundle removed
- Changed from static class to instance-based with `QiZhengStarPositionStatusRepository` injection.
- Registered in `di.dart` as `Provider<YuanLePanelBuilder>`.
- `_loadStarPositionStatusData()` now reads from `_positionStatusRepo.loadStarPositionStatus()` (contracts) instead of `rootBundle.loadString()`.
- UI call sites (`yuan_le_result_panel.dart`, `yuan_le_result_panel_v2.dart`) updated to `context.read<YuanLePanelBuilder>()`.

### 5.4 ZhouTianModelManager — fallback removed
- `.instance` singleton removed.
- Constructor now requires non-nullable `QiZhengZhouTianModelRepository`.
- `_loadBuiltinPresets()` method deleted (was the rootBundle fallback path).
- `load()` now always reads from injected `_repository`.
- `flutter/services.dart` import removed.

### 5.5 GeJuManager — dead code deleted
- `lib/domain/managers/ge_ju/ge_ju_manager.dart` deleted.
- Zero references in `lib/` — actual GeJu evaluation goes through `GeJuEvaluationService → IGeJuRepository`.

### 5.6 Unused Flutter imports removed
- `package:flutter/foundation.dart` removed from: `star_to_star_relationship_model.dart`, `ge_ju_evaluation_service.dart`, `ge_ju_input_builder.dart`, `ge_ju_evaluator.dart`.
- `debugPrint` calls replaced with `print` in those files.
- `package:flutter/rendering.dart` already removed in Q3 (`save_calculated_panel_usecase.dart`).
- `package:flutter/services.dart` removed from `generate_base_panel_service.dart` (unused import).

### 5.8 Domain Flutter deny-list
```
rg -n "package:flutter/|rootBundle" lib/domain/
→ 0 matches
```

### 5.9 Domain data-layer deny-list
```
rg -n "(^|/)data/|LocalDataSource|RepositoryImpl|SystemDefinitionLocalDataSource" lib/domain/
→ 0 matches
```

### 5.10 Architecture boundary tests
```
flutter test test/architecture/import_boundary_test.dart
→ All 6 tests passed
```
No new analyzer errors in touched domain files.

### BeautyPageViewModel — Q5 migration complete
`lib/presentation/pages/beauty_page_viewmodel.dart` has zero `data/**` imports.
All ShenSha/HuaYao data loading is delegated to domain-layer managers and services.
Dead code (commented-out `_initializeUseCases`, `_database`, `SaveCalculatedPanelUseCase` construction) removed.
