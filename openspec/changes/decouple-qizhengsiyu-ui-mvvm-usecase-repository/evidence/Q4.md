# Q4 Main Pan ViewModel Migration Evidence

- **Date:** 2026-06-14
- **Branch:** `codex/qizhengsiyu-mvvm-usecase-repository-plan`
- **Result:** GO

## Verification

1. **UseCase Injection in di.dart:**
   - 5 UseCases registered as providers: `InitializeQiZhengOfficialDataUseCase`, `CalculateQiZhengBasePanelUseCase`, `EvaluateQiZhengGeJuUseCase`, `BuildQiZhengTimelineUseCase`, `SaveCalculatedPanelUseCase`.
   - `QiZhengSiYuViewModel` constructor now receives UseCases instead of domain managers (`ShenShaManager`, `HuaYaoManager`, `ZhouTianModelManager`, `GeJuEvaluationService`).
   - `flutter analyze lib/di.dart lib/presentation/viewmodels/qi_zheng_si_yu_viewmodel.dart` → 0 errors.

2. **ViewModel Delegate to UseCases:**
   - `init()` delegates to `_initUseCase.execute()`.
   - `calculateWithConfig()` delegates to `_calculateUseCase.execute()` for base panel, `_buildTimelineUseCase.execute()` for timeline/DaXian.
   - `evaluateGeJu()` delegates to `_evaluateGeJuUseCase.execute()`.
   - `calculateDaXian()` delegates to `_buildTimelineUseCase.execute()`.
   - Legacy `ValueNotifier` getters backed by UseCase results, maintaining UI compatibility.

3. **Data Imports Removed from QiZhengSiYuViewModel:**
   - Removed: `data/datasources/local/hua_yao_local_data_source.dart`, `data/repositories/hua_yao_repository_impl.dart`, `data/datasources/local/shen_sha_local_data_source.dart`, `data/repositories/shen_sha_repository_impl.dart`.
   - Removed: direct imports of `ShenShaManager`, `HuaYaoManager`, `ZhouTianModelManager`, `GeJuEvaluationService`, `GenerateBasePanelService`, `CalculationEngineFactory`, `GeJuInputBuilder`.
   - `rg "(^|/)data/|rootBundle|RepositoryImpl|LocalDataSourceImpl" lib/presentation/viewmodels/qi_zheng_si_yu_viewmodel.dart` → 0 matches.

4. **Architecture Boundary Tests:**
   - `flutter test test/architecture/import_boundary_test.dart` → All 6 tests passed.
   - UI boundary, ViewModel boundary, UseCase boundary, Domain Flutter boundary, Domain data boundary all clean.

5. **BeautyPageViewModel:** Data imports remain in `beauty_page_viewmodel.dart` (4 data/** imports + rootBundle calls). Documented as Q5 target.

6. **Legacy ValueNotifier Compatibility:** All existing `ValueNotifier` getters (`uiZhouTianModelNotifier`, `uiBasePanelNotifier`, `uiDaXianPanelNotifier`, `uiBasicLifeStarsNotifier`, `uiFateLifeStarsNotifier`, `geJuSummaryNotifier`, `birthRiseSetNotifier`, `customRiseSetNotifier`, `lunarDateInfoNotifier`) retained as compatibility facades.

## Known Remaining (Q5 Scope)

- `BuildQiZhengTimelineUseCase` still directly constructs `GenerateBasePanelService` with `ShenShaManager`/`HuaYaoManager` — Q5.1-Q5.4 domain cleanup.
- `BeautyPageViewModel` still has data/** imports and `rootBundle.loadString` calls — Q4.7/Q5 target.
- `SwephEngine.rootBundle.loadString` — Q5.1.
- `ZhouTianModelManager._loadBuiltinPresets()` still uses `rootBundle` in legacy path — Q5.4.
